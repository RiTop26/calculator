# calculator
For this project, I am going to combine everything I have learned so far by making an on-screen calculator using JavaScript, HTML, and CSS

Create functions to handle basic math operators such as add, subtract, multiply, divide

Create a function operate that take an operator and two numbers and calls one of the above function on the numbers

Create a basic HTML calculator with buttons for each digit and operator including equal and clear buttons

Create functions that will make every button works and add a click event on them

